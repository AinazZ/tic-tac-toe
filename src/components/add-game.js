import React, { Component } from 'react';

import './add-game.css';

export default class AddGame extends Component {
  render() {
    return (
      <button
        className="addGame"
        //onClick={() =>}
      >
        +
      </button>
    );
  }
}
